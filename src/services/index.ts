// This file is for reusable API functions.
// For example, functions for contact form submission, lead capture, etc.
// Currently, contact form submission is handled by Next.js Server Actions in 'src/app/contact/actions.ts'.

// You can export server actions from here if desired for a centralized API module:
// export { submitContactForm } from '@/app/contact/actions';

// Or define other client-side API utility functions.
// Example:
// export async function fetchSomeData(params: any) {
//   const response = await fetch(`/api/some-endpoint?${new URLSearchParams(params)}`);
//   if (!response.ok) {
//     throw new Error('Failed to fetch data');
//   }
//   return response.json();
// }
